[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
request_macro = { path = "../request_macro" }
reqwest = {version = "0.11.13", features = ["json"]}
tokio = {version = "1.22.0", features = ["full"]}
serde_json = "1.0.89"
serde = {version = "1.0.148", features = ["derive"]}